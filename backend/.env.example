DATABASE_URL=postgresql://readonly_user:readonly_password@localhost:5432/chinook

# Never commit your real API key.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# Comma-separated origins allowed to call this API. Add your deployed
# frontend's URL here once it's live (defaults cover local dev only).
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
